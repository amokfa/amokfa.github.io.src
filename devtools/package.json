{
  "name": "devtools",
  "version": "1.0.0",
  "description": "",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "serve": "node serve.js",
    "deploy": "npm run build && git -C ../_out add . && git -C ../_out commit -a -m \"$(date)\" && git -C ../_out push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "exec-sh": "^0.4.0",
    "fs-extra": "^10.0.0",
    "http-server": "^13.0.1",
    "open": "^8.2.1"
  }
}
